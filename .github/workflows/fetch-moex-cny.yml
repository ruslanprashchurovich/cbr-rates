name: Fetch MOEX CNY rate

on:
  schedule:
    # Время в cron — UTC (МСК = UTC+3). Каждые 10 минут; минуты не «круглые»,
    # чтобы не стоять в общей очереди на раннеры.
    # Хотите каждые 5 минут — замените на "4-59/5 * * * *".
    - cron: "4-59/10 * * * *"
  workflow_dispatch: {} # кнопка ручного запуска во вкладке Actions

permissions:
  contents: write

# Общая группа с workflow курсов ЦБ: оба коммитят в main,
# поэтому выполняем их строго по очереди, чтобы не гонялись за push.
concurrency:
  group: fetch-rates
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Fetch CNY rate
        run: python3 fetch_moex_cny.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/cny_rub.csv
          if git diff --cached --quiet; then
            echo "Курс не изменился — коммитить нечего."
            exit 0
          fi
          git commit -m "cny: $(date -u +'%Y-%m-%d %H:%M') UTC"
          git push || (git pull --rebase origin main && git push)
